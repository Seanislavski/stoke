# CLAUDE.md narrative archive — extracted 07/26/2026

Verbatim sections moved out of `CLAUDE.md` to shrink the per-session context load.
Nothing here is deleted; the live CLAUDE.md keeps a condensed version of each with a pointer back to this file.

---

# Archived: Q&A Knowledge Base / Question of the Week

## Q&A Knowledge Base (replaces Resources tab — 06/11/2026)
- **Concept:** community Q&A as durable, searchable "external memory" — members ask questions + contribute answers; BOTH queue for mod approval; only approved content is viewable. Replaces the old flat link/photo Resources tab. Piloted with Body Doubling (premiere/flagship community before official Stoke launch). Origin: Fable 5 phone convo (saved in Obsidian `+ Encounters/Fable 5 and Sean.md`) — "the knowledge exists; the retrieval fails."
- **Tables** (migration `20260611000000_knowledge_base.sql`):
  - `kb_categories`: community_id, name, description, position, created_by — organizer-defined; assigned to a question at approval time
  - `kb_questions`: community_id, category_id (FK→kb_categories on delete set null), asker_id, title, body, status (published/pending/rejected), approved_by, published_at
  - `kb_answers`: question_id, community_id (denormalized), author_id, body, url, status, is_accepted bool, approved_by, published_at
- **PostgREST FK hint REQUIRED:** kb_questions has asker_id + approved_by both → profiles, kb_answers has author_id + approved_by both → profiles ⇒ ambiguous joins → always use `profiles!asker_id(...)` / `profiles!author_id(...)`
- **RLS:** select published-only + insert-own; all pending reads + mod writes go through `createAdminClient()` (same pattern as bulletin/resources). No `is_community_mod` helper used.
- **Decisions locked:** ranking = accepted-answer marking ONLY, NO upvotes/karma (Fable warning: gamifying "attract[s] transactional behavior + repel[s] genuine helpers"); tab name = "Q&A" (reserving "Ask" for the future real-time requests flow); both Q + A need approval (high question volume expected, many rejected); accepted answer = asker OR mod, one per question (`toggleAcceptAnswer` clears all then sets)
- **Files:** `app/actions/knowledge.ts` (all actions + `modEmails()`/`emailFor()` helpers); `components/knowledge/` (KnowledgeBoard=client instant search+category-chip filter; AskQuestionForm, AnswerForm, QuestionModActions=approve+file-category in one step, AnswerModActions, AcceptAnswerButton, CategoryManager); detail route at `questions/[questionId]/page.tsx`
- **Recategorize-after-approval (07/02/2026):** Sean approved a question but "forgot to choose a category first" — category was previously ONLY settable at approval time (via `QuestionModActions`), no way back in once published. Added: `setQuestionCategory(questionId, communityId, slug, categoryId)` action in `knowledge.ts` (mod-only, same auth as `approveQuestion`, audited + revalidates both `/communities/{slug}` and the question path); `components/knowledge/QuestionCategoryPicker.tsx` (client, auto-saves on `<select>` change — "Saving…"/"Saved", reverts on error, renders null if 0 categories); wired into `questions/[questionId]/page.tsx` for `isMod && status==='published'` (pending questions still use QuestionModActions). Audit label `question.recategorized` → "Changed a question's category" added to `lib/audit.ts` ACTION_LABELS. `npx tsc --noEmit` clean. **Committed `37438f1` on main (bundled with the YouTube-embed work below) and PUSHED — live in prod** (verified 07/03: `main == origin/main`, nothing ahead).
- **UX:** Q&A tab = mod pending-questions review section + KnowledgeBoard (search/filter) + AskQuestionForm; pending answers reviewed inline on the question detail page (contextual). Client-side instant search for v1 (no tsvector); answer authors credited with profile links ("close the loop visibly").
- **Email templates** (`lib/email.ts`): kbQuestionSubmitted/Approved, kbAnswerSubmitted/Approved. **Audit actions** (`lib/audit.ts`): question.created/submitted/approved/rejected/deleted, answer.created/submitted/approved/rejected/deleted/accepted.
- **Question of the Week (07/03/2026 — commit `34583e9`, local-not-pushed):** Sean starts posting a weekly question on Stoke Mondays + pointing Discord to it (the "shrink the per-touchpoint ask" funnel lever). Sean: *"I think it can have its own area on Stoke (not necessarily its own page, but maybe within the Q&A section?)"* → built INSIDE Q&A, no separate page. **Chosen landing = public per-question view** (out of 3 options I offered; opt2=block-on-preview, opt3=members-only-ruled-out).
  - **NO migration** — QOTW is derived from a Q&A **category named exactly `Question of the Week`** (`lib/qotw.ts` `findQotwCategoryId()`, case-insensitive, scoped per community). Current spotlight = newest published question in that category (boardQuestions already ordered `published_at desc` → first match). Past weeks accumulate there as an answerable archive. Mod-controlled (askers don't set category; only mods/owner do at ask/approve time) so members can't populate it by accident.
  - **Spotlight card** pinned atop the Q&A tab (`communities/[slug]/page.tsx`, orange gradient, "⭐ Question of the Week · N answers · Add yours →"); renders only when `qotwSpotlight` exists.
  - **Public read-only view** `app/preview/[slug]/questions/[questionId]/page.tsx` — logged-out visitors see the question + published answers + a "Sign up to answer" CTA (`/signup?redirect={canonical}` → lands them on the real answerable page post-auth). `middleware.ts` **rewrites** logged-out `/communities/{slug}/questions/{id}` → `/preview/{slug}/questions/{id}` so the shared URL stays canonical (mirrors the bare-slug preview rewrite; logged-in users fall through to the real gated page). **Privacy guard:** the public page redirects to `/communities/{slug}` unless the question is published AND in the QOTW category — so ONLY the QOTW is exposed, rest of the (unlisted) Q&A stays private (mirrors featured-reviews "explicit publish-publicly" principle).
  - LinkPreview logged-out safety: YouTube→iframe + image→img need NO API; generic-link OGCard fetches the auth-gated `/api/link-preview` and **degrades to null** (renders nothing) logged-out → safe to reuse `RichContent` on the public page.
  - **Setup switch (one-time):** the `Question of the Week` category must EXIST in a community for any of this to activate (spotlight hidden + public route redirects until then). The category was CREATED 07/03 via `scripts/add-qotw-category.mjs` (id `996c2934-afe4-4223-b7b0-0eefb07fa222`) + the morning feature PUSHED live (commit `93be1e1`, both Railway services SUCCESS). Monday flow (morning-feature version): Ask a question → pick the QOTW category (owner auto-publishes) → instant spotlight + live public link.
- **Question of the Week BACK-END (07/03/2026 — commit `c87c531`, local-not-pushed, SUPERSEDES the morning ad-hoc flow):** Sean wanted a management page to *"store questions in there if I think of one well in advance, and then... select one and with a click of a button assign the question to be QotW"* + *"assign ones for future weeks... pre-loaded for (potentially) months!"* Then REVISED the share model: he wants *numbered permanent links* (`example.link/QotW-1`) so questions are *"look[ed] up in the future"* and — critically — *"there is no time that someone MUST answer by. I don't like that, and I think most Neurodivergents would agree with me. This also means that if someone is bored, they can answer previous QotW's."* So: NO deadlines, every QotW stays open forever, whole back-catalog answerable.
  - **New table `qotw_items`** (migration `20260703000000_qotw_items.sql`): community_id, title, body, `number` int (QotW-N, null while draft, assigned on publish, unique per community), `planned_for` date (organizational LABEL only — does NOT auto-publish), `question_id` (fk kb_questions, set on publish), position, created_by, published_at. RLS-on-no-policy (all access via service-role admin client). **⚠️ migration NOT yet run — feature inert until it is** (code is fail-safe: `qotw_items` reads use `.then(r=>r.data??[])` so a missing table → empty bank, nothing crashes; spotlight is unchanged since it reads kb_questions).
  - **Model:** Bank (drafts, number=null) → **one-click Publish** (`publishItem`: assigns next number = max+1, creates the answerable kb_question in the QOTW category authored by owner/auto-published, links question_id). Deliberately **manual publish, NO cron/auto-rotation** — Sean's no-deadline philosophy made auto-release optional; "pre-loaded for months" = stockpile the bank + click weekly. (Offered auto-weekly-release as a small follow-on if he wants zero clicks.)
  - **Files:** `app/actions/qotw.ts` (addDraft/updateDraft/deleteItem/publishItem, `requireMod` gate mirrors knowledge.ts); `components/qotw/QotwManager.tsx` (client: add-to-bank form, bank list w/ edit/delete/publish + planned-week label, published list w/ QotW-N + copy-link/delete; imports server actions directly — not via `.bind` props); management page `app/(app)/communities/[slug]/qotw/page.tsx` (mod-gated); numbered redirector `app/(app)/communities/[slug]/qotw/[n]/page.tsx` (logged-in → real question); public numbered view `app/preview/[slug]/qotw/[n]/page.tsx` (logged-out, "QotW-N" label + Sign-up-to-answer, mirrors the morning per-question preview). `middleware.ts` rewrites logged-out `/communities/{slug}/qotw/{n}` → `/preview/{slug}/qotw/{n}` (bare `/qotw` = management, stays mod-gated). Settings page has a "Question of the Week → Manage" link section (after Q&A categories). Audit labels `qotw.published`/`qotw.deleted`. tsc + `next build` clean (3 new routes registered).
  - **"Preview as test" publish (07/03, commit `d558049`, PUSHED live d558049 both services SUCCESS):** Sean wanted to test the full flow *"without using the QotW-1 (maybe use QotW-t instead?)"*. Added a throwaway test publish using **sentinel number `0`** → renders as **"QotW-t"** at `/qotw/0` via shared `qotwLabel(n)` helper (`lib/qotw.ts`: `n===0 ? 'QotW-t' : 'QotW-'+n`, `QOTW_TEST_NUMBER=0`). `publishItem` got a 4th arg `asTest=false`; real publishes now use `max(number > 0)+1` so the test-0 NEVER inflates real numbering (Monday's first real = QotW-1 whether or not the test is deleted). QotwManager has a secondary "Preview as test" button next to "Publish as QotW". Delete the QotW-t row when done → spotlight clears. Caveat told to Sean: while QotW-t is live it IS a real published kb_question so members see it as the current spotlight (fine for a quick pre-Monday test).
  - **Test-copy FIX (07/03, commit `4f16bfa`, pushed):** first version of `asTest` UPDATED the draft in place → the bank row *became* QotW-t, so deleting the test would delete the question. Sean hit this ("I'm afraid that I will delete it?") — reverted his first test via a one-off script (number/question_id→null + delete the throwaway kb_question). Fixed: `asTest` now INSERTS a separate throwaway COPY, original draft stays in the bank; deleting QotW-t only removes the copy. Verified live: 24 bank drafts intact after a second test. **TWO delete paths that confuse (Sean asked why the warning differed):** (1) the **manager** QotW-t row Delete = clean + friendly confirm ("Remove this QotW-t test? Your original question stays safe in the bank"); (2) the **question detail page** generic mod delete ("Delete this question and its answers?", `DeleteItemButton`→`deleteQuestion` in `questions/[questionId]/page.tsx`) = deletes the test's kb_question but orphans the qotw_item (question_id→null via FK ON DELETE SET NULL). Guide users to the MANAGER delete. Possible follow-up: suppress/redirect the generic delete for QotW-numbered questions, or have it clean the qotw_item too.
  - **Self-serve for ANY community (07/03, commit `2a3c346`, PUSHED live):** Sean asked *"how is QotW a useable system (if desired) for other communities?"* — answer: the app was already fully multi-tenant (qotw_items scoped by community_id, pages by slug, numbering per community, "Manage" link on every community's settings). The one friction was the manual exact-name category requirement. FIXED: `ensureQotwCategory(admin, communityId, userId)` in `qotw.ts` find-or-creates the "Question of the Week" category on first publish → organizers never need the magic name; removed the "create a category first" warning + button gating from QotwManager (dropped the `hasCategory` prop + the `cats` fetch from the manage page). Also DOCUMENTED QotW in the Organizer Guide — new "Question of the Week" section in BOTH `apps/web/app/guide/page.tsx` (+ TOC entry `#qotw`) and `docs/running-a-community.md` (private bank, one-click publish, numbered links, no deadlines, Preview as test). Only BD-specific bits left are the two seed scripts (hardcode slug `bodydoublingcom`).
  - **Model-choice aside:** Sean asked *"is this something that I should switch to Fable 5 for?"* → I said no for the BUILD (coding/agentic = Opus 4.8's strength), yes for the CONTENT/strategy layer (writing the actual weekly questions = where he already uses Fable 5). Principle: Opus for the plumbing, Fable for the words. Kept Opus 4.8.
- **Per-question public toggle (07/05/2026 — built, NOT yet committed/pushed):** Sean posted an unanswered member question to Discord, then noticed that opening it logged-out showed the private-community screen (Image #1) instead of the question. Root cause = deliberate design: the public preview page (`preview/[slug]/questions/[questionId]/page.tsx`) `loadPublicQuestion` guard exposed ONLY published questions in the QotW category; everything else redirected to `/communities/{slug}` (→ unlisted-community private screen). Sean: *"I want the question itself to be visible even to those who are not yet logged in — can we somehow make that the default?"* Offered 3 scopes (any published Q / listed-communities-only / per-question toggle); **Sean chose "Just add a per-question toggle"** — private by default, mod flips individual questions public. Built:
  - **Migration `supabase/migrations/20260705000000_kb_question_public.sql`** — `alter table public.kb_questions add column if not exists is_public boolean not null default false;`. **⚠️ MUST RUN BEFORE PUSHING** — new code selects `is_public`; if the column is missing, PostgREST errors → `question` null → the logged-in question detail page 404s for everyone. Column-add is backward-compatible (deployed code doesn't reference it) so run it early, then push.
  - `setQuestionPublic(questionId, communityId, slug, isPublic)` in `knowledge.ts` — mirrors `setQuestionCategory` auth (`requireModAccess`), audited `question.made_public`/`question.made_private` (labels added to `lib/audit.ts`), revalidates `/communities/{slug}` + the question path.
  - Public guard now exposes a question if published AND (`category_id === qotwCategoryId` OR `is_public`). `loadPublicQuestion` returns an `isQotw` flag; the "⭐ Question of the Week" eyebrow renders only when actually QotW, else "{community.name} · Q&A"; metadata desc generalizes off the QotW "this week" copy. QotW no longer requires a category to exist for the page to work (dropped the early `if (!qotwCategoryId) return` bail).
  - `components/knowledge/QuestionPublicToggle.tsx` (client, checkbox auto-saves + reverts on error, shows the canonical share URL + Copy-link when on). Wired into `questions/[questionId]/page.tsx` for `status==='published' && isMod && !isQotw` (QotW questions are already public via their numbered link, so no toggle there). Detail-page question query gained `is_public`.
  - No middleware change (existing question-URL rewrite already sends all logged-out question hits to the preview). tsc clean. Migration RUN by Sean 07/05 (Supabase success). Committed+pushed `97e4ac6`.
  - **Answers gated behind sign-up (07/05, follow-up):** Sean: *"the question should (with the toggle on) be visible to all, but the answers should only be visible by logged-in users. I think this will encourage people to sign up."* On the public preview page, a public question shows the question + an answer-COUNT teaser ("🔒 N answers have been shared. Sign in to read them" + Sign-up button) instead of the answer bodies. Then Sean: *"The QotW answers also should be gated"* → gating is now UNIVERSAL on the public page (QotW included) — removed the `isQotw` branch in the answers block. The `loadPublicQuestion` answers query was trimmed to `.select('id')` (count only) so answer bodies are never even pulled into memory, let alone the HTML. Commits `b46c631` (initial non-QotW gate) then `a02f6d3` (universal gate, count-only query). All PUSHED — Railway redeploying. Toggle copy later trimmed: dropped "and its answers" (wrong — answers are gated) `31153d7`, then dropped the Discord mention `b142e58` (Sean: not applicable to most communities).
  - **Asker public-sharing preference + against-preference confirm (07/05, built, PUSH HELD pending migration):** Sean wants the ASKER to signal consent at post time without granting them control: *"when someone is posting the question, they should be able to select a box indicating that they are okay with sharing it publically - and it should be selected by default... If it has been selected or not simply indicates to moderators what the choice is, and if they choose to go 'against' the asker's preference, they have to click on a new pop up which says something like 'Are you sure? This goes against what was indicated at the time of initial posting.'"* So the asker still CANNOT publish (mod-only `setQuestionPublic` unchanged) — the checkbox is purely an advisory signal.
    - **Migration `supabase/migrations/20260705010000_kb_question_asker_pref.sql`** — `add column if not exists asker_public_pref boolean;` (nullable; NULL = legacy/no-pref). **⚠️ MUST RUN BEFORE PUSHING** (code selects + inserts it → missing column breaks question submit + the detail page). NOT yet run.
    - `AskQuestionForm.tsx`: checkbox `public_ok`, **defaultChecked**, shown to all askers ("I'm okay with this question being shared publicly. Organizers decide whether to actually publish it; this just records your preference. (Answers always stay members-only.)"). `submitQuestion` stores `asker_public_pref: formData.get('public_ok') === 'on'`.
    - Detail page passes `askerPref={question.asker_public_pref ?? null}` to `QuestionPublicToggle`. Toggle shows the recorded preference (✔ okay / ⚠ prefers private) and, on any toggle change where `askerPref !== null && next !== askerPref`, fires `window.confirm('Are you sure? This goes against what the asker indicated at the time of initial posting.')`. Initially SYMMETRIC; Sean then chose ONE-WAY — the confirm now fires ONLY on the privacy-sensitive direction (`next && askerPref === false`, i.e. making public when the asker preferred private); turning a question back private is never blocked. tsc clean. Migration run 07/05; `39bb9d1` pushed, one-way tweak follows.
- **Promote a member's question to QotW in place (07/05/2026 — LIVE, no migration):** Sean: *"How can we use questions that have been submitted by others as QotW?"* Gap found: the bank's `publishItem` always CLONES a fresh owner-authored question, so before this the only way to QotW a member question was re-categorizing it into the "Question of the Week" category (spotlight only, credited to them, but NO number/`/qotw/N` link). Built `publishExistingQuestion(questionId, communityId, slug)` in `qotw.ts` (mod-gated via `requireMod`): promotes an EXISTING kb_question in place — assigns next `QotW-N` (`max(number>0)+1`), `ensureQotwCategory` + files it there, publishes it if pending (sets status/approved_by/published_at; **asker_id untouched → original member keeps credit**), inserts a `qotw_items` row linked to that `question_id` (gets the permanent numbered link + becomes spotlight). Errors if it's already a QotW (existing qotw_items row for that question_id). `components/qotw/PublishAsQotwButton.tsx` (client, window.confirm, `router.refresh()`), wired into `questions/[questionId]/page.tsx` for `isMod && !isQotw && status !== 'rejected'` (works for pending too — promotes+approves in one). After promote the page's existing `isQotw` path takes over (⭐ QotW-N badge + "Manage in QotW →", public toggle hidden). Audit `qotw.published`. tsc clean, pushed `2f87e3b`.
  - **Congratulate the asker (07/05, LIVE, no migration):** Sean: *"If a member's question is highlighted and chosen as QotW, they should receive a notification congratulating them."* `publishExistingQuestion` now inserts a `type='qotw'` notification for `question.asker_id` (skipped if the promoter IS the asker) — user_id=asker, actor_id=mod, community_id, `message_id=questionId` (channel_id null). `NotificationsBell.tsx` made qotw-aware: renders "⭐ Your question was chosen as Question of the Week!" and click routes to `/communities/{slug}/questions/{message_id}` (the other types still route to the channel via `?mention=`). Only the promote-EXISTING path notifies; the bank `publishItem` is owner-authored so there's no member to congratulate. tsc clean. Congrats notification `474c17e`.
  - **Congrats EMAIL too (07/05, LIVE, no migration):** Sean: *"add the congratulatory email too."* Added `qotwChosenHtml(communityName, communitySlug, questionId, questionTitle, numberLabel)` to `lib/email.ts` (orange ⭐ card, "featured as QotW-N with its own permanent link"). `publishExistingQuestion` sends it fire-and-forget (`void (async()=>…)()`, never blocks) alongside the bell notif: fetches the asker's email via `admin.auth.admin.getUserById(askerId)` + community name/slug, subject `Your question is the Question of the Week in {name}`, label via `qotwLabel(nextNumber)`. Skipped when promoter===asker (same guard). tsc clean.
- **QotW scheduler — hybrid auto-publish (07/05/2026 — LIVE code, needs a cron-job.org entry to actually fire):** Sean asked *"What would it take to have a scheduling system for QotW, so that future QotW can be scheduled?"* Answer: ~70% already existed — `qotw_items.planned_for` (date, was label-only), the `/api/cron/*` infra (CRON_SECRET bearer + middleware exclusion + cron-job.org, same as event-reminders), and the publish logic. Sean chose the **Hybrid (date wins, else rotate)** cadence model (over date-only or weekly-only).
  - **No migration.** New `lib/qotw-publish.ts` (plain server module, NOT 'use server') holds `ensureQotwCategory` (MOVED out of `qotw.ts` — both now import it) + `releaseDraft(admin, communityId, item, actorId?)`: the real-publish core (next number = max(number>0)+1, ensure category, insert kb_question authored by community **owner**, link qotw_items in place, audit `qotw.published`); `actorId` optional → defaults to owner so a session-less cron can call it. `qotw.ts` trimmed to `import { qotwLabel, QOTW_TEST_NUMBER }` + `ensureQotwCategory` from the lib. (Left `publishItem`'s own real/test branch as-is to avoid touching the live manual flow — `releaseDraft` is cron-only; minor numbering-logic duplication, both identical.)
  - **New cron route `app/api/cron/qotw-publish/route.ts`** (GET, CRON_SECRET auth, mirrors event-reminders). Per community, at most ONE release/run: (1) **date wins** — earliest bank draft whose `planned_for <= today` (UTC date string compare) publishes; (2) **else rotate** — if no dated draft due AND `now - last QotW published_at >= 7 days` (ROTATE_GAP_MS), publish the next UNDATED draft in `position` order. Idempotent (publishing assigns a number → never re-picked). Revalidates each released community's `/` + `/qotw`.
  - `QotwManager.tsx` copy updated: "Planned week" → "Auto-publish date (optional)" + explainer (dated = auto-publish that day, undated = weekly auto-rotation, manual publish still available); bank badge "Planned:" → "⏱ Auto-publishes {date}".
  - **⏳ REMAINING (Sean's one manual step):** add a cron-job.org job — daily, `GET https://stoke.community/api/cron/qotw-publish`, header `Authorization: Bearer <CRON_SECRET>` (same secret as event-reminders). **CRON_SECRET value is NOT recorded here (this file is in the repo)** — it lives in `apps/web/.env.local`, Railway env vars, and Obsidian `+ Encounters/Claude Session 5-29-26.md` (~line 2130); all three match (generated `openssl rand -hex 32`, 05/29). Until then the code is inert (nothing calls the route). Scheduler publishes BANK drafts (owner-authored) so no congrats notif (those are only for promoted member questions). Rotation is a rolling 7-day gap anchored to last publish (not a fixed Monday); pin exact weeks via dates. tsc clean, pushed.
  - **Cron CONFIGURED + first-launch plan (07/05/2026):** Sean created the cron-job.org job (its own `America/New_York` timezone, so no UTC math). Initially `0 13 * * *` (1pm ET); told him to change the time dropdown 13→**08** (`0 8 * * *`) so it fires **Mon Jul 6 2026 8:00 AM ET** — his Monday-morning cadence. **Live state verified via read-only service-role scripts (07/05):** Body Doubling has **24 bank drafts, 0 published (number>0), all undated** → because the rotate gate is open (no prior publish), the FIRST cron run will auto-publish the top undated draft = **"What's one task you've been avoiding — and what's the smallest possible first step on it?"** as **QotW-1** (owner-authored, no consent issue). After that, ~weekly rotation (7-day gate) at 8am Mondays.
  - **Consent-driven decision:** Sean deliberately did NOT make the member question **"Cleaning and organizing habits/systems that stick?" (pinkflamingo, already published)** the QotW-1 — *"As I wasn't gathering the checkbox info when this was published yesterday, I won't start with that one"*: it predates the `asker_public_pref` consent checkbox, so there's no record she's okay with public featuring. Good privacy instinct — promoting it would have publicly featured her question + emailed her without recorded consent. (The ⭐ Publish-as-QotW button WOULD have worked and made it QotW-1 since 0 are published; he chose the owner draft instead.)
  - Reminders to Sean: confirm the job's URL ends `/api/cron/qotw-publish` (not the event-reminders job), and leave event-reminders at `*/10 * * * *`. CRON_SECRET also confirmed present in Railway (matches `.env.local` + Obsidian note).
  - **Reorderable queue UI (07/10/2026 — built, NOT yet committed):** Sean: *"can we start a queue of future Questions of the Week, and have them automatically triggered in the order that they are in in the queue?"* — answer: the auto-fire-in-order mechanism ALREADY existed (the cron's "rotate" branch publishes the next undated draft in `position` order, ~7 days apart). The gaps were (1) NO reorder control in the UI (`position` = insertion order only) and (2) the manager framed the bank as a passive stockpile, no "next up" cue. Sean chose to **keep the weekly (~7-day) cadence** (over fixed-day-of-week). Built (no migration, no cron change): `reorderBank(communityId, slug, orderedIds)` in `qotw.ts` (mod-gated, renumbers `position` 0..n over drafts where `number is null`); `QotwManager.tsx` renamed "Bank"→**"Queue"**, added **▲▼ reorder arrows** (optimistic local `order` state synced to the `bank` prop via `useEffect`, `move(i,dir)` swaps + calls `reorderBank`), a position number, and an **"⏭ Next up" badge** (orange ring) on the first UNDATED draft in queue order = what rotation publishes next (dated drafts jump the line on their date, so they're not "next up"). Copy now reads "publish automatically top to bottom — one per week (~7 days apart)… give any question a date to make it jump the line." tsc clean. No changelog entry (staff-only organizer tool). Awaiting Sean's go to commit + push.
  - **✅ QotW-1 IS LIVE (07/06/2026):** the scheduled 8/9am cron run did NOT fire — root cause: **the cron-job.org job was TOGGLED OFF (disabled)** (Sean: *"it's as if I had never created the job"* → found it existed but disabled; enabled it + set 9am ET, but the scheduled trigger still didn't fire and showed next-run=tomorrow). Sean used cron-job.org's **"TEST RUN" button** (manual trigger) → it hit the route live → published **QotW-1** ("What's one task you've been avoiding…") at 13:06 UTC, owner-authored, linked, no consent issue. **KEY:** a successful Test Run PROVES the URL+Bearer+secret are all correct (a bad header = 401 = publishes nothing) — so the config is valid; the miss was purely the off-toggle. Verified via read-only service-role script: 1 published (QotW-1), 23 drafts remain; public link `stoke.community/communities/bodydoublingcom/qotw/1` = 200 logged-out, no login-wall, "QotW-1" label + question present. Shared to Discord (mod nudge + link). **Going forward:** daily 9am cron fires but only PUBLISHES when the 7-day rotate gap elapses → next auto = QotW-2 ~Jul 13; interim daily runs return `released:0` (correct, not a bug — glance at the job's execution-history 200 to confirm the auto-trigger works). **✅ CONFIRMED 07/10:** the daily cron IS firing (execution history shows 200) — the auto-trigger works, so QotW-2 will auto-publish once the 7-day gap elapses (~Jul 13); no more manual Test Run needed.
  - **QotW-2 miss diagnosed + gate fixed + live countdown (07/13/2026 — built, NOT yet committed):** Sean: *"I don't think the new QotW has fired, I thought it would around 9am - perhaps we should build a better way to see/visualize what time is left before the switch and have it wired in to the actual time the switch happens."* **ROOT CAUSE (confirmed via read-only diagnostic against live data):** QotW-1 was published **07/06 at 13:06:24 UTC** (via cron-job.org's Test Run button, hence the off-9am time). The old rotate gate opened at *exactly* `last_published_at + 7d` = **07/13 13:06 UTC = 9:06 AM ET**; the daily cron fires at **9:00 AM ET (13:00 UTC)** → it ran **6 min BEFORE the gate opened** (6.99 days elapsed), correctly published nothing. Systemic fragility: a millisecond-precise gate checked once/day at a fixed clock time drifts later each week and sporadically skips. **FIX (2 parts):**
    - **NEW `lib/qotw-schedule.ts`** — single source of schedule math used by BOTH cron + UI so the countdown can't disagree with reality. `ROTATE_GAP_MS`, `QOTW_CRON_HOUR_ET=9`, `rotateEligibleDate(last)` = `date(last+7d)` (UTC, **date-granularity**), `isRotateGateOpen(last)` = `todayUTC >= eligibleDate`, `nextCronSwitch(eligibleDate)` = first future 9am-ET instant on/after eligible (reuses `wallTimeToUtcIso` from `eventTime.ts` for DST-safe ET→UTC), `resolveNextPublish({lastPublishedAtISO, undatedNext, datedNext})` → `{id,title,at,reason:'date'|'rotate',gateOpen}` (earliest of date-wins vs rotate).
    - **Cron `route.ts`** rotate gate swapped from `(Date.now()-published)>=ROTATE_GAP_MS` (ms) → `isRotateGateOpen(last?.published_at)` (date-level). This makes the daily cron reliably fire on the eligible day AND **stabilizes the cadence to the same weekday ~9am ET every week** (verified by simulation: locks to Tuesdays going forward) instead of drifting.
    - **NEW `components/qotw/NextPublishPanel.tsx`** (client) — top of the QotW manager page: next question + **live ticking countdown** + exact ET instant ("Tue, Jul 14, 9:00 AM EDT") computed server-side via `resolveNextPublish`. `gateOpen && !overdue` → nudge to Publish now. Wired in `communities/[slug]/qotw/page.tsx` (computes `lastPublishedAt` from rows where `number>0`, `undatedNext`/`datedNext` from bank). tsc clean.
    - **Immediate state:** gate is OPEN now (7.07 days) → with the fix deployed QotW-2 (`"What's a 'weird' trick that actually works…"`) auto-publishes **Tue Jul 14 9:00 AM ET**; or Sean can Publish-as-QotW now. Staff-only organizer tool → **NO changelog entry** (per rule).
    - **✅ COMMITTED + PUSHED `b538b02` (07/13, Sean said "yes")** — 4 code files only (`lib/qotw-schedule.ts`, `components/qotw/NextPublishPanel.tsx`, cron `route.ts`, `qotw/page.tsx`); left `CLAUDE.md`/`.claude/`/PDF/`images/` OUT (pre-existing untracked). `main == origin/main`, Railway redeploying. Committed to main directly (project convention: launched product but Sean confirmed the push).
    - **Status-bar window labels (07/13):** Sean asked to make the status bar show which window is which ("is this the Stoke window?" → "can you make that show on the status bar?"). The user-global `C:/Users/Sean/.claude/statusline.js` ALREADY reads a friendly name from `<dir>/.claude/name` (or `.projectname`) and shows it BOLD in place of the dimmed folder name when it differs. Created `C:/Users/Sean/OneDrive/reciprocal-community-platform/.claude/name` = `Stoke` → status bar now `Opus 4.8 · Nk/200k ctx (N%) · Stoke`. Script reads the file fresh each render (event-driven) → no restart needed. Per-window: Plish/Spark/Silas still show their folder names until each gets its own `.claude/name` (offered to add them). NOTE: this repo's `.claude/` is untracked in git (harmless if committed).
  - **Live traction reframe (07/07):** QotW-1 had only Sean's own answer day-1. Corrected a STALE metric (I'd quoted "~11 signups" from launch week) → **real current: 35 signups, 32 active BD members, 18 signups last-7-days, ~2-3/day trickling** — top-of-funnel is HEALTHY; low QotW answers = engagement/friction + day-1, not pool-size. Lever = seed the empty room to 4-5 answers (social proof) + direct DMs to recent signups ("I'd love YOUR take" > broadcast) + "one sentence is fine". Sean posted a mod-nudge in mod-chat (link-preview card rendered = public QotW OG metadata works). LESSON (again): never quote a metric from memory for a "current" read — pull live data.
- **DEFERRED sibling feature:** real-time "Ask / I can help" requests flow (ephemeral: "partner at 2pm" → tap "I can help" → fulfilled). Designed KB as a clean sibling; NOT built. Scope decision was "Both, knowledge base first."
- **Body Doubling community:** slug `bodydoublingcom`, id `5310e8c7-1276-485f-b77e-406d7edcf890`, owner_id `4e216ab6...` (Sean's account; also owns dorky-platypus-lovers + dracula-fans). **⚠️ LIVE STATE 07/26: `is_listed = true`, `join_mode = 'open'`** — verified by service-role probe. This file previously said "currently unlisted"; that is STALE and has been corrected in all three places. Anything reasoning about Body Doubling's privacy (preview teasers, the signed-in-non-member question gate, `/library` plans) must use the LIVE values, not this note — re-probe if it matters.
- **Seed script:** `scripts/seed-bodydoubling-qa.mjs` — 6 categories + 6 starter questions (the recurring Discord ones), authored by owner so auto-published. Idempotent; `--remove` to undo. Already run successfully.
- **Live Q&A state (inspected 07/02/2026, read-only via a temp `scripts/` script using service-role key + kb_categories/kb_questions/kb_answers):** 6 categories; 7 questions (6 published seed + **1 pending, member-asked** "Cleaning and organizing habits/systems that stick?"); 7 answers, **ALL authored by members not owner** (real unprompted activity — encouraging) — 6 published + **1 pending** (an answer on "Morning routine ideas"). Per-question answer counts: "Best apps/extensions for blocking distractions?" = 3; "Morning routine ideas" = 2 (1 pub/1 pending); "How do I start…" = 1; "How do I run a good body doubling session?" = 1; **"What timer setup actually works for ADHD?" = 0** and **"What do you do when you lose focus mid-session?" = 0** (empty); **no accepted answers marked anywhere**. **This REVISES the conversion/funnel fix ("#1 seed the room"): the room is NOT empty — members are contributing.** Refined 3 gaps, priority order: (1) ⚡ approve the 2 items stuck in the mod queue (1 question + 1 answer) — instant fill + rewards contributors, pure upside, but publishes member content so Sean reviews text first; (2) fill the 2 zero-answer questions with real Discord advice (Claude can't invent it); (3) mark accepted/best answers for a "resolved library" signal. LESSON: don't assume the room is empty — inspect live data first; my initial "seed the empty room" framing was wrong.


---

# Archived: Bulk-Add Members

## Bulk-Add Members (07/15/2026 — built, NOT yet committed as of session end)
- **Trigger:** Sean, live at a GRACE weekly meeting: *"how can I best put in the info for new users who are part of the GRACE group?"* → *"We are at our weekly meeting right now, and I want to easily onboard them, and will play the part of the guy on the computer, and verbally get the information I need from them to create their accounts."* Then: *"generate a page where I can bulk-add members by filling out a spreadsheet-like form."*
- **Email confirmation is OFF on Stoke's Supabase** (verified empirically 07/15 via a throwaway anon-key signup probe → `access_token` returned immediately, probe user deleted via service-role admin). So admin-created accounts work instantly — no inbox/confirm step. (This is the key fact that makes operator-driven account creation viable.)
- **New page `app/(app)/communities/[slug]/bulk-add/page.tsx`** — server component, gated to **owner / organizer / platform-staff** (mirrors "who can email all members" — heavier than everyday mod). Reachable from **Settings → Members → "+ Bulk-add members"** link (added to settings `page.tsx` members section header).
- **`components/community/BulkAddMembers.tsx`** (client): spreadsheet-like grid **Username · Email · Password**; auto-grows a blank row as you type the last row; **paste from Excel/Sheets** (`onPaste` parses tab/newline blocks, fills from the focused cell, expands rows); a **"Shared temporary password"** field + "Apply to all rows" (the live-meeting pattern — one temp pw like `Grace2026!`, members change it later); per-row status badges (✓ Created / ✓ Added / • Member / ✗ error) + a summary.
- **`app/actions/bulk-members.ts`** `bulkAddMembers(communityId, slug, rows)`: builds an email→id map once via `admin.auth.admin.listUsers({perPage:1000})` (detects dupes + lets existing accounts be added); per row → `admin.auth.admin.createUser({ email, password, email_confirm:true, user_metadata:{ username, display_name:username } })` (the `handle_new_user` trigger auto-creates the profile from that metadata), then insert/reactivate a `community_members` row (`role:'member'`, `status:'active'`). **Bypasses join_mode** — adds directly as active (GRACE is `join_mode:'request'`, irrelevant here). Respects `checkMemberLimit` per new member; skips banned; leaves existing active members' role untouched. Audits `member.added` per add (label added to `lib/audit.ts`). tsc clean.
- **GRACE community:** name "GRACE (Grassroots Arts & Community Education)", slug `grace-grassroots-arts-community-education`, `join_mode:'request'`, owner = Sean's `4e216ab6-...` account (same as bodydoublingcom). Bulk-add URL: `stoke.community/communities/grace-grassroots-arts-community-education/bulk-add`.
- **Staff-only organizer tool → NO changelog entry** (per curation rule).
- **✅ SHIPPED + PUSHED (07/15):** commit `b12e951` (bulk-add page/grid/action + `member.added` audit label + settings link), then `d2fae29` (welcome email). Left `CLAUDE.md`/`.claude/`/PDF/`images/` OUT of the commits (pre-existing untracked/modified, per convention). Sean said "deploy" — Railway rebuilding. `main == origin/main`.
- **Welcome email (07/15, `d2fae29`):** Sean asked *"once I add people, will they receive an email?"* → answer was NO (`admin.auth.admin.createUser` sends nothing; the action calls no `sendEmail`), then *"Yes please"* to adding one. New `bulkWelcomeHtml(communityName, communitySlug, email, tempPassword)` template in `lib/email.ts` (login details box + "Log in to {community}" button → `/login?redirect=/communities/{slug}` + change-password nudge). Fired fire-and-forget (`void sendEmail(...)`) **only for newly-CREATED accounts** (`created===true`) — they got a temp password they need to know; existing accounts added to the community get nothing (they have their own login). Action now fetches `communities.name` once for the subject/body. **⚠️ Emails the PLAINTEXT temp password** (acceptable — throwaway, changed on first login; flagged to Sean). Only sends if `RESEND_API_KEY` set in Railway (it is). tsc clean.
- **Verified fact (07/15):** `admin.auth.admin.createUser({ email_confirm:true })` creates a confirmed user and sends NO email by default (unlike `inviteUserByEmail`). Confirmation is separately OFF at the project level too (probed empirically this session).
- **Deploy verified live (07/15):** bulk-add route returns **307** (auth-gate redirect to /login) not 404 → deployed + working. GOTCHA: `curl` returns `000` for ALL stoke.community URLs in this sandbox (curl TLS/network broken here) — a background poll on 404→non-404 gave a FALSE "live" via 000. **Use PowerShell `Invoke-WebRequest -SkipHttpErrorCheck` (or `-MaximumRedirection 0`) for status checks on this box, not curl.**
- **Test welcome email sent (07/15):** Sean asked to see the real contents → sent the actual `bulkWelcomeHtml` (sample pw `Grace2026!`) to `baldwinseana@gmail.com` via Resend REST (`POST https://api.resend.com/emails`, Bearer `RESEND_API_KEY` from `.env.local`, Resend id `9c1fd8a1…`, HTTP 200). Script replicated the template verbatim (wrap+btn+bulkWelcomeHtml) — did NOT touch the DB / create an account. GOTCHA: the `resend` SDK import path in a standalone node script is fiddly — **just hit the Resend REST API with fetch** instead of importing the SDK.


---

# Archived: Channels / Gathering Spaces

## Channels / Gathering Spaces
- `channels` table: community_id + name + description + position + created_by
- `messages` table: channel_id + author_id + content + edited_at; added to `supabase_realtime` publication
- Realtime: browser client subscribes to `postgres_changes INSERT` filtered by `channel_id`
- Channel page: server loads last 50 messages + profile cache; client handles realtime + sending
- Messages grouped Discord-style: consecutive same-author messages collapsed
- **Message editing (07/10/2026 — built, NOT yet committed):** Sean: *"I see that I can delete a message after it has been sent to a channel, but what if I want to edit it?"* — the `edited_at` column + Message type field already existed; delete was built but edit wasn't. Built (NO migration): `editMessage(messageId, channelId, communityId, content)` in `app/actions/messages.ts` — **author-only** (mods can delete but not reword others' words, mirrors the Q&A "mods delete only, no rewording" rule); blocks editing a deleted message; blocks emptying a text-only message (image-only may keep empty text); sets `content` + `edited_at`; audit `message.edited` (label added to `lib/audit.ts`). `ChannelView.tsx`: pencil button (author-only) beside the trash — desktop hover-reveal in the avatar gutter (`flex-col`, stacked over trash) for same-author collapsed rows / in the header for first-of-author rows; mobile always-visible cluster on the right; inline edit form (input + Save/Cancel, Enter saves / Esc cancels, optimistic + rollback); "(edited)" muted marker after edited content. **KEY FIX:** the realtime UPDATE handler previously only synced `deleted_at`/`deleted_by` (built for delete/restore) → a content edit would NOT propagate to other viewers; rewrote it to also carry `content` + `edited_at` (non-mods still drop a message on delete; everyone syncs edits in place). `edited_at` already in the channel page's initial select so "(edited)" shows on load. tsc clean. Changelog 07-10 entry added (member-facing). SHIPPED commit `180b1d5`, pushed.
  - **Edit audit before/after (07/10, follow-up):** Sean: *"is the edit in the audit log, and can you see the before and after there?"* — it WAS logged (`message.edited` + `View →` to the message) but the `View →` only shows the CURRENT text and the metadata captured only `channel_id`, so the original was lost. Fixed: `editMessage` now stores `metadata: { channel_id, before, after }` (fetches old `content` before the update; added a `trimmed === content` no-op guard). BOTH audit surfaces (`AuditLogClient.tsx` + settings `page.tsx`) render a `before̶ → after` line (strikethrough old, `truncEdit()` helper trims to 140 chars, collapses whitespace) for `message.edited`. Staff-only surface → NO changelog entry. NOTE: only edits made AFTER this deploy carry before/after; any pre-fix edit shows just the label. tsc clean. SHIPPED `f4719aa`, pushed.
  - **Undo last edit (07/10, follow-up):** Sean: *"once a comment (is that what we should call them?) is edited, is it difficult to make that edit revertable by the author?"* — naming: call them **messages** (table `messages`, "comment" implies threaded post replies). Chose **single-level undo** (over full version history / leave-as-is). **Migration `20260710000000_message_previous_content.sql`** = `alter table messages add column if not exists previous_content text` (nullable, backward-compatible → run before deploy; Sean ran it, "success"). `editMessage` now also writes `previous_content = <old content>`; NEW `revertMessage(messageId, channelId, communityId)` (author-only) swaps content back + **clears `previous_content`** (so only ONE level back until the next edit) + `edited_at=now`, audits `message.reverted` (label added; both audit surfaces' before/after render extended to `message.reverted`). `ChannelView`: Message type + INSERT/UPDATE realtime casts + initial select + optimistic-message all carry `previous_content`; edit's optimistic update stashes `previous_content=original.content` so the **"↩ Undo edit"** button (author-only, shows when `previous_content != null`, next to the "(edited)" marker) appears instantly; `handleRevert` optimistic + rollback. NOTE: revert keeps "(edited)" showing + sets a fresh `edited_at` (it WAS just modified) — truthful even though content matches a prior state. Changelog 07-10 "Undo edit" item added (member-facing). tsc clean.
- **Message replies + reply notifications (07/10/2026 — built):** Sean: *"you should be able to reply to a message, and get a notification/ping if someone replies to you directly."* Chose **Discord-style single-level inline reply** (a message references one other message; small quoted reference above it) — fits the flat `messages` model + the Discord-native 13k audience; NOT deep threads. **Migration `20260710010000_message_replies.sql`** = `alter table messages add column if not exists reply_to_id uuid references messages(id) on delete set null` (soft-deletes keep the parent row so the quote persists; ON DELETE SET NULL only bites on hard delete). NEW `app/actions/replies.ts` `notifyReply(replyMessageId, replyToId, channelId, communityId)` — notifies the PARENT message's author (skips self, active-members-only, 5/hr cap; mirrors `processMentions`); inserts a `type='reply'` notification (channel_id + message_id = the REPLY's id → bell routes via `?mention=` to the reply). `NotificationsBell` renders `type==='reply'` as " replied to you in #channel". `ChannelView`: `reply_to_id` added to Message type + INSERT/UPDATE realtime casts + initial select (`channels/[channelId]/page.tsx`) + optimistic message; `replyingTo` state; a **Reply button** (↩, everyone, non-deleted/non-optimistic) in all three action clusters (gutter/header/mobile); `handleSend` includes `reply_to_id` + fires `void notifyReply(...)` when replying to someone else; a **composer reply bar** ("Replying to {name} — {snippet}" + ✕, Esc cancels); a **quoted reference** atop each reply (↩ {name} {60-char snippet}) that click-scrolls to the parent via `scrollToMessage` (reuses the purple mention pulse). Parent lookup = `messageById` over loaded messages (older/unloaded parent → "original message" fallback). NO audit (regular message send isn't audited). Changelog 07-10 reply items added. tsc clean. Awaiting Sean's go to commit + push (migration must be run first).
  - **Reply-jump pulse fix (07/11/2026 — built, push pending):** Sean: *"when you click on that smaller version [the quoted reference], the original should pulse."* The mechanism existed but (1) the pulse was near-invisible (`bg-purple-50` + hairline outline + Tailwind `animate-pulse` = a faint whole-row opacity dim on a near-white bg) and (2) clicking the SAME reply twice was a no-op (`setMentionedId(id)` with an unchanged value skips the React re-render → animation never restarts). Fixed in `ChannelView.tsx`: `scrollToMessage` now `setMentionedId(null)` then re-sets on `requestAnimationFrame` so re-clicks restart the pulse; the flash bumped to `bg-purple-100 ring-2 ring-purple-300` (blue audit-jump highlight bumped identically to `bg-blue-100 ring-2 ring-blue-300`). tsc clean. Member-facing polish on the reply feature — offered a changelog line under the existing 07-10 reply entry.
- **Message reactions (07/05/2026 — LIVE, `98c4be1` pushed; migration run in Supabase 07/05; curated 8-emoji set kept per Sean, full picker deferred):** Sean got his first Stoke @mention (someone "@Sean"'d him in a new Introductions channel in Body Doubling) and noticed Stoke chat had no emoji like Plish. Root cause: Stoke ≠ Plish (separate codebase); `ChannelView` chat was built lean (image attach + @mention + Send only). Sean chose **reactions** over a plain emoji-insert picker.
  - **Migration `supabase/migrations/20260705020000_message_reactions.sql`** — `message_reactions` (id, message_id FK messages ON DELETE CASCADE, **channel_id denormalized** FK channels, user_id FK profiles, emoji text, `unique(message_id,user_id,emoji)`); RLS mirrors messages (select/insert on active membership of the channel's community, delete-own); **`replica identity full`** so realtime DELETE payloads carry channel_id/message_id/emoji (default ships only PK → would break the filter + not tell us which reaction to drop); added to `supabase_realtime`. Migration RUN 07/05 (Supabase success).
  - channel_id is denormalized because a `postgres_changes` filter only keys off a column on the SAME table (can't reach message_id→messages.channel_id). Client subscribes `reactions:{channelId}` filtered `channel_id=eq.{channelId}` for INSERT + DELETE.
  - **`ChannelView.tsx`:** new `initialReactions` prop (page loads reactions for the 50 messages via admin `.in('message_id', ids)`); `reactionPills(id)` groups → ordered `{emoji,count,mine}`; `toggleReaction` insert/delete via the supabase CLIENT under RLS (like message-send) w/ optimistic + rollback; realtime dedup by (message_id,user_id,emoji). UI: pill row under each non-deleted/non-optimistic message (mine=orange, click toggles) + hover-reveal (`md:opacity-0 md:group-hover:opacity-100`, always visible mobile) smiley button → popover of `['👍','❤️','😂','🎉','😮','😢','🙏','🔥']`; fixed z-20 backdrop closes picker (popover z-30). tsc clean.
  - **Reaction notifications (07/05, coalesced + throttled — Sean picked this over one-ping-per-reaction or none, for the ADHD/low-noise reason):** **NO migration** — `notifications.type` is plain text + inserts go via service-role admin client (no INSERT policy on the table). New `app/actions/reactions.ts` `notifyReaction(messageId, channelId, communityId)`: notifies the message AUTHOR, never self; **coalesces** — if the author already has an UNREAD `type='reaction'` notif for that message, it UPDATEs it silently (actor_id + created_at) instead of inserting (the bell only subscribes to INSERT, so UPDATE never re-pings); else per-actor hourly cap of 5 (mirrors `processMentions`), then INSERT. Called fire-and-forget (`void notifyReaction(...)`) from `toggleReaction`'s ADD path only (not un-react) after a successful insert. `NotificationsBell.tsx` made type-aware: `n.type === 'reaction' ? ' reacted to your message in ' : ' mentioned you in '` (click still routes to the message via `?mention=`). tsc clean.
- **`!` community-link picker (07/07/2026 — DESIGN LOCKED, build PARKED):** Sean asked whether typing a symbol + a community name in a channel should autolink to that community. **Agreed symbol convention:** `#` = channel (Discord parity — the 13k audience are Discord natives where `#`=channel, so DON'T use `#` for communities), `@` = user mentions (unchanged), **`!` = community picker**. **PARKED because low utility now** — only 3 communities exist and 2 have just their creator, so there's essentially nothing to cross-link; cross-community discovery is the whole point, so build when a 2nd community has real members. **Locked build spec when revived:**
  - **Picker half is easy — mirror the existing `@` machinery in `ChannelView.tsx`** (which already drives mentions): the composer detects a token via `textBefore.match(/@(\w*)$/)` → `mentionQuery` state → `mentionSuggestions` dropdown → arrow/Enter → `completeMention` swaps `@(\w*)$` for `@username `. For communities: add a parallel detector `!([\w-]*)$` (only fires at a token boundary — preceded by whitespace/start, so "done!" never triggers it), feed it a **community list** instead of the member list, and `completeCommunity` inserts a token.
  - **⚠️ STORE THE COMMUNITY `id` (UUID), NOT THE SLUG** (decided 07/07 when Sean asked about editable slugs — see below). A slug is potentially mutable, so a message must NOT embed it. Insert a token like **`!!{communityId}`** (or `!<uuid>`) into `messages.content`; the human-visible name/slug are looked up fresh at render time. This makes every past mention survive a slug rename automatically (same principle as not storing display names in text).
  - **Rendering is the only real work:** `@username` renders as plain text today, but the community token must render as the community's **current display NAME** and be a clickable `<Link href="/communities/{currentSlug}">`. Resolve **`id → current name + current slug → <Link>`** at render time. Pass the linkable communities (`id, slug, name`) from the channel page server load into `ChannelView` as a prop (only a handful of communities → trivial to pass the whole set + build an **id→{name,slug}** map). Do the token tokenization in a **channel-scoped** message renderer woven around `RichContent` (which does its own URL parsing) — do NOT add this parsing to the global `RichContent` (would misfire on `!word` in bulletin/Q&A/etc.).
  - **Which communities appear in the picker (privacy):** listed communities + the user's own memberships (so unlisted communities the user isn't in never surface by name). No cross-community notification needed (unlike @mentions) — it's just a link.
- **Editable community slug (07/07/2026 — DESIGN NOTE, not built):** Sean asked if a "customize your slug" field should live in the community menu. Reasonable — natural home = **Settings → General (`CommunityInfoForm`)**, input under the name with validation (lowercase + hyphens, **unique** across communities, block reserved words `new`/`mine`/etc. which `middleware.ts` special-cases). **The catch = "cool URLs don't change":** the slug is the identifier in EVERY URL — community page, `/preview/{slug}`, invite landings, and the **QotW numbered public links `/communities/{slug}/qotw/N`** actively shared in Discord. Editing a slug 404s every previously-shared link (Discord posts, bookmarks, search indexing). So if built, pair it with either (a) an **old→new redirect / slug-alias table** (store slug history so old links keep resolving — preferred if the QotW links matter), or at minimum (b) a **clear warning at edit time**. This is WHY the `!`-picker above must store the community id, not the slug.


---

# Archived: Reviews / Testimonials

## Reviews / Testimonials (06/23/2026 — commit 4b83f22)
- **What:** members leave reviews (optional 1–5 stars + text); mods approve them, then **feature** a curated few that surface publicly as testimonials. Sean: "members can leave their reviews/feedback, and admins can choose a few to be displayed to attract users." Purely public testimonials (private feedback stays in tickets).
- **One `reviews` table, scope by nullable `community_id`:** NULL = a platform-level review of Stoke; non-null = a community review. (Mirrors `tickets.community_id` nullable.) Columns: id, community_id, author_id, rating (int nullable, 1–5), body, status (pending/published/rejected), is_featured bool, approved_by, published_at, created_at, updated_at. Migration `supabase/migrations/20260623000000_reviews.sql`. **⚠️ migration must be run manually in Supabase — not yet run as of 06/23.**
- **Two-tier:** `status` pending → published (visible in-app on the Reviews tab to members) → `is_featured` (the curated subset shown PUBLICLY). Mirrors the Q&A (`knowledge.ts`) approve pattern; pending reads + all writes via `createAdminClient()`.
- **Edit → re-approval (Sean's requirement):** `editReview` forces `status='pending', is_featured=false, approved_by=null, published_at=null` in the same update → an edited review instantly drops from ALL public + in-app published display until a mod re-approves. Re-notifies mods. "Once edited, reviews are auto-removed from public display until they are re-approved."
- **Eligibility:** community review = active member (mod/owner auto-publishes); platform review = any logged-in user (platform mod auto-publishes). Platform-scope "mod" = owner/platform_moderator only.
- **One review per author per scope** — enforced with TWO partial unique indexes (`where community_id is not null` and `where community_id is null`) because NULLs are distinct in a plain unique constraint.
- **PostgREST:** `reviews` has author_id + approved_by both → profiles ⇒ all joins use `profiles!author_id(...)`.
- **Surfaces:** Reviews tab on `communities/[slug]` (members + mod approve/feature inline); featured reviews on `preview/[slug]` ("What members say", shown regardless of is_listed — featuring is an explicit publish-publicly act); platform testimonials on `app/page.tsx` landing ("What organizers are saying", **renders nothing when zero featured** → stays hidden until real ones exist post-launch); member platform-review entry at `/feedback` (gear menu "Share your experience"); platform curation at `/admin/reviews` (canModerate).
- **Files:** `app/actions/reviews.ts`, `components/reviews/{ReviewForm,ReviewList,ReviewsManager}.tsx`, `lib/reviews.ts` (shared `REVIEW_COLS` + `mapReview`), `app/(app)/feedback/page.tsx`, `app/(app)/admin/reviews/page.tsx`. Audit actions `review.*` in `lib/audit.ts`; email templates `reviewSubmittedHtml` (mods) + `reviewFeaturedHtml` (author) in `lib/email.ts`.

### Review management for organizers (06/23/2026 — commit e1262ad, migration `20260623120000_review_replies_ordering.sql`)
- Sean: "I want to help the organizers manage their reviews." Chose **all four** capabilities, in **community settings** ("matches my mental model"). Mod controls REMOVED from the member-facing Reviews tab (now display-only + "manage in settings" hint); the tab stays clean for members.
- **`ReviewsManager.tsx`** (client) is the management surface, used in BOTH community settings AND `/admin/reviews` (platform, communityId=null). Sections: sentiment snapshot (avg rating + 5→1 distribution bars), status filter tabs (pending/published/featured/rejected/all with counts), per-row approve/reject/feature/delete + reply, featured ▲▼ reorder.
- **Featured cap = 6** — `toggleFeatureReview` blocks featuring a 7th ("You can feature up to 6 reviews — unfeature one first"); UI also disables the button at cap. `featured_position` int orders the featured set; `reorderFeatured(communityId, slug, orderedIds)` renumbers. Public surfaces (preview/landing) order by `featured_position asc` then `published_at desc`, limit 6.
- **Organizer replies** — `setReviewReply(reviewId, communityId, slug, formData)` (empty body removes the reply). Columns `reply_body, reply_is_public, reply_by, reply_at`. **Public** reply (`reply_is_public=true`) shows wherever the review shows (member tab + preview + landing); **private** only to the review author + community staff. Visibility helper `canSeeReply(review, viewerIsStaff, viewerUsername)` in ReviewList; member tab passes `viewerIsStaff={isMod}` + `viewerUsername` (derived from members list). One reply per review (business-response style). Editing a review still re-queues it (reply persists, hidden while review pending).
- **Gear badge** now sums pending join requests + pending reviews: `pendingCount={(pendingCount ?? 0) + (pendingReviewsCount ?? 0)}` on `CommunityGear` (no gear component change).
- `ReviewModActions.tsx` was DELETED (superseded by ReviewsManager). `ReviewList.tsx` is now display-only (exports `ReviewItem`, `Stars`, `canSeeReply`, `ReplyBlock`).
- Audit labels added: `review.reordered`, `review.replied`, `review.reply_removed`.


---

# Archived: Static Assets, Hosted Docs & Launch

## Static Assets, Hosted Docs & Launch (06/23/2026)
- **Middleware matcher must exclude EVERY hosted static file extension.** `apps/web/middleware.ts` `config.matcher` negative-lookahead originally excluded only `svg|png|jpg|jpeg|gif|webp`, so `.pdf`/`.html` files in `apps/web/public/` hit the auth gate → logged-out visitors got redirected to `/login`. Fixed by adding `pdf|html|ico|txt|xml|webmanifest`. **LESSON:** any new public static file type must be added to that list, and ALWAYS test hosted assets while **LOGGED OUT** (Invoke-WebRequest is effectively logged-out; verify `Content` starts with `%PDF-`, not just HTTP 200 — the login shell returns 200 `text/html` ~13KB with no `type=password` because the form is client-rendered, which is easy to mistake for success).
- **PDF generation from HTML (zero deps):** headless Chrome/Edge print-to-pdf. `chrome.exe --headless=new --do-not-de-elevate --disable-gpu --no-first-run --user-data-dir=<temp> --no-pdf-header-footer --print-to-pdf=<out> file:///<in.html>` via PowerShell `Start-Process -Wait -NoNewWindow`. Chrome at `C:\Program Files\Google\Chrome\Application\chrome.exe`; Edge at `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`. `--no-pdf-header-footer` strips the default URL/date header.
- **⚠️ ELEVATED-TERMINAL GOTCHA (07/01/2026) — the `--do-not-de-elevate` flag is REQUIRED.** Sean's project Terminal profiles run **elevated** (`"elevate": true`). Chrome 149+ (and Edge) **auto-de-elevate** when launched as admin: they silently relaunch themselves NON-elevated and the process you started exits 0 **immediately with no PDF written** — no error, nothing (the tell in verbose `--enable-logging=stderr --v=1` output is a `Windows.AutoDeElevateResult` histogram line). This is what silently broke a prior PDF session ("long wait then the terminal closed"). Fix = pass Chrome's own internal `--do-not-de-elevate` flag so it stays put and renders headless as admin. Also wrap the launch in a **kill-timeout** so a hang can't take the terminal down: `$p = Start-Process ... -PassThru -NoNewWindow; $p | Wait-Process -Timeout 60 -EA SilentlyContinue; if(-not $p.HasExited){$p | Stop-Process -Force}`. NOTE: `-NoNewWindow` and `-WindowStyle` are mutually exclusive in `Start-Process` (don't pass both). Render to a UNIQUE temp filename then `Copy-Item` over the target (the public PDF gets locked while open in a viewer). AVOID `Remove-Item` on paths under `C:\Program*` (trips a path-protection guard). Single-page layout: `.page { height:11in; overflow:hidden; display:flex; flex-direction:column }` + `.body { justify-content:space-between }` to distribute whitespace evenly.
- **Join how-to guide:** `apps/web/public/how-to-join-body-doubling.{html,pdf}` → live at `stoke.community/how-to-join-body-doubling.{html,pdf}`. One-page branded guide for the OPEN-join launch flow. Wording (per Sean): NOT "new/better home" — it's "a reliable place to build a shared, lasting library of tools and tips for neurodivergent minds."
- **Launch:** ✅ LAUNCHED 06/29/2026 (Discord announcement went out via the mod team). **Signups are slow: ~5 on day 1 (06/29), ~6 on day 2 (06/30) → ~11 total.** Sean is underwhelmed given the 13k Discord. My framing to him: 11 off a *soft* resource-channel launch is not a product verdict — it's early + low-reach + "join another platform" is a high-friction ask (esp. ADHD audience). Before changing anything, diagnose WHERE the funnel leaks: (1) reach — how many actually SAW the post (soft housekeeping post ≠ 13k impressions); (2) click-through — did the hook land (gauge by post reactions/replies); (3) signup completion — preview→signup on-site. Two bets regardless: there must be something pulling signups BACK (seed real Q&A activity so a visitor sees a live room, not empty), and one post is never "the launch" (needs 3–4 repeat touchpoints). ⏳ Waiting on Sean to say how it actually posted (@everyone vs soft) + the post's reaction/reply count to pick the lever. BodyDoubling.com is the ONLY live community (public, `join_mode=open`). Sean still plans to switch join_mode back to closed after the initial wave — at which point the how-to guide's instant-join steps need updating or the link taken down.
- **⚠️ LAUNCH-POST FACTS CORRECTED 07/01 (Sean shared the actual #announcements screenshots) — the "soft launch / low reach" premise was WRONG.** The announcement was a full **`@everyone` blast in #announcements** (06/29 9:38 AM), re-posted `@everyone` again 06/30, plus a softer 06/30 12:51 PM nudge ("Thanks for those who have joined… check out how asking/answering works… DM me"). It carried Sean's locked copy + both links + rich embeds ("Stoke Community — A place where everyone has something to offer" / "BodyDoubling.com on Stoke"). **Engagement was STRONG: ~42 reactions (🔥18, 😍7, 👍5, ✨4, ❤️3, +others).** So the earlier "mods folded it into a calmer housekeeping post, likely no big ping" note (06/28) did NOT reflect what actually posted. **Re-diagnosis with real data: reach = HIGH ✅, hook/resonance = STRONG ✅ (people loved the idea), signup completion = LOW ❌ (~11).** The leak is **CONVERSION (interest→action), i.e. the intention-action gap** (frictionless 🔥 vs the high-friction "leave Discord, make an account, learn how it works" — steep for ADHD). This is the *encouraging* read: the idea is validated; the fix is friction, not messaging. **Levers, prioritized:** (1) SEED the room — a 🔥-reactor who lands on an empty Q&A bounces; run/verify `seed-bodydoubling-qa.mjs` + add real answers from the Discord thread so first visit shows a LIVING library; (2) shrink the per-touchpoint ask — "answer this one question" beats "explore a platform" → the new **Question of the Week** PDF is exactly this repeat-touchpoint engine now the @everyone card is spent; (3) work the ~18 🔥-reactors as warm leads (targeted follow-up/DM). LESSON: don't record a launch's reach/ping from a *plan* or a mod *intention* — confirm from the actual posted message.
- **Discord launch announcement (drafted 06/28, ready to post):** Sean iterated the copy into his own voice. Locked framing (Sean): NOT "new/better home" → "a reliable spot to build a shared, lasting library of tools and tips that work for our minds" + "our community's external memory." Links posted in announcement: `stoke.community/communities/bodydoublingcom` (the public preview, has Join CTA) + the guide PDF `stoke.community/how-to-join-body-doubling.pdf`. Open decision at post time: the ping (`@here` online-only vs `@everyone` 13k vs none) — recommended `@here`. **Mod-team reframe (06/28):** mods folded the announcement into a calmer server-housekeeping post — "new text channels coming to the resources & server-guide category": (1) a *Voice Channel Descriptions* channel and (2) a resource channel that's "a guide to access" the shared library. Softer/embedded rollout → likely NO big ping (lives in a resource channel people discover, not an @everyone blast). ⚠️ **CORRECTED 07/01 — the "tense mismatch" was MY misread, there is NO mismatch.** I thought "A shared library" in the intro referred to Stoke (live now), clashing with "coming over the coming days." Sean corrected me: **"A shared library" is a forthcoming *Discord channel*** (a resource channel that will be a guide to accessing Stoke), NOT Stoke itself. So the intro (Discord channels, all genuinely future) and the body ("come take a look" at Stoke, genuinely live) are two different subjects on correct timelines — internally consistent. LESSON: don't flag a "contradiction" without confirming what each noun refers to. (Only residual, trivial + left alone: the phrase "shared library" names the future *channel* in the intro and the live *platform* in the body — same words, two referents, but coherent.) Optional copy nit: "premiere" vs "premier" (debut vs flagship) — Sean uses "premiere"; left to his call.
- **Both launch URLs verified LOGGED-OUT 06/28** (the 06/23 middleware static-file fix is holding): community link → 200, no redirect, serves the preview not the login wall (has "Get started" / "already have an account", NO password field); PDF → 200 `application/pdf`, 67KB, `%PDF-` magic bytes. Verify technique: a bare 200 `text/html` is NOT proof (login shell is also 200 ~13–23KB) — check content markers + absence of `type=password`; for PDFs check `%PDF-` magic bytes, not just status.
- **Silas! ↔ Q&A KB (future idea, NOT built):** Sean asked if the shared library "is where Silas! works." Functionally NO — Silas! is the Discord Pomodoro **voice** bot (separate system/platform); the library is the Stoke web Q&A KB. But thematically a great match (Silas! = librarian theme + a lasting library). Future possibility: Silas! pointing members to the KB. Don't conflate them in launch comms (would confuse where Silas! lives).


---

# Archived: Discord Capture Pipeline (Silas! to Stoke) + photo work

## Silas! ↔ Stoke: Discord Capture + Consent Pipeline (07/23/2026 — PLAN LOCKED, NOTHING BUILT)
- **Trigger:** Sean saw a Discord post worth persisting: *"advice that needs to persist. I want to make it easy to get people's permission and then to post it on Stoke, tagging them (if they give permission)"* → then *"we potentially build a… Discord bot that enables capturing information for Stoke… use this as a potential funnel (if the poster wants) and I want a system that ensures their permission is recorded."* Then: *"can we somehow integrate Silas into Stoke?"* → YES, decided: **extend Silas! (not a new bot)** — the librarian persona is the perfect wrapper ("the librarian asked to shelve your post"). Silas stays a BD character; if the machinery later generalizes to other communities, the persona stays BD's.
- **5-phase plan (agreed, awaiting "go"):**
  - **P0 — Silas system user on Stoke: ✅ DONE 07/23** — `scripts/create-silas-user.mjs` (idempotent) created auth user `silas@stoke.community`, profile `@silas` ("Silas!", librarian bio), active BD member. **SILAS_USER_ID = `28184489-f70d-4943-a050-fc51e833905b`** — capture filing authors content as this user. (Mirrors Plish's system-user pattern; random never-used password.) No avatar yet — upload one via profile settings or a follow-up script.
  - **P1 — Migration: ✅ RUN 07/23** (Sean confirmed "success" in Supabase; verified right project first via `select slug from communities` sanity check). `supabase/migrations/20260723000000_discord_captures.sql` — `discord_captures` table (RLS-on-no-policy = service-role only, like qotw_items): community_id, discord_message_id (unique) / channel_id / message_url, discord_author_id + author_name, content snapshot, captured_by_discord_id, `consent_status` (pending/granted_credited/granted_anon/declined), consent_asked_at/answered_at, `claim_token` (unique), claimed_by→profiles, question_id/answer_id (null until filed). PLUS nullable `attribution text` on kb_answers + kb_questions (rendered instead of author profile link when present).
  - **P2 — Silas bot: ✅ CODE BUILT 07/23, NOT yet committed/deployed** — `src/capture.js` (new, in Silas repo; Supabase via plain fetch, zero new deps) + `index.js` routing + `deploy-commands.js` ContextMenuCommandBuilder (default-gated ManageMessages) + `.env.example`. All `node --check` clean. Dormant until Railway env vars set: `SUPABASE_URL` (Stoke!), `SUPABASE_SERVICE_ROLE_KEY`, `STOKE_COMMUNITY_ID=5310e8c7-1276-485f-b77e-406d7edcf890`, optional `CAPTURE_MOD_ROLE_ID` (fallback = ManageMessages perm), `STOKE_URL`. Command registration after deploy: `railway run npm run deploy`. Consent PATCH filters `consent_status=eq.pending` so an answer can never be overwritten (race-safe). DECISION: hold bot deploy until P3+P4 exist (else granted captures have no inbox). Details: message **context-menu command "📚 Capture for Stoke"** (guild-scoped, mod-role-gated) → inserts pending row + ephemeral confirm. **Consent DM** to the author with 3 buttons: ✅ credit / 👤 anon / ❌ no; handler verifies clicker id === discord_author_id (makes in-channel fallback safe when DMs closed). On credit-yes → follow-up with claim link `stoke.community/claim/{token}`. Declined rows KEPT (record that we asked + honored no). New Silas Railway env: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (Stoke's project) — dormant without them (CHECKIN_CHANNEL_ID pattern). **Mod does NOT pick destination in Discord** — capture+consent only.
  - **P3 — Stoke: ✅ CODE BUILT 07/23, tsc clean, NOT yet committed** — `app/actions/captures.ts` (publish-as-answer/question authored by SILAS_USER_ID + attribution, pre-approved; discard blocked once published; requireMod mirrors knowledge.ts) + `components/community/CaptureActions.tsx` (filing UI, done-in-place) + moderation page "Discord captures" section (credited/anon label, consent date, original-↗ link) + gear badge `totalPending` += granted-unfiled captures + question page renders "📚 Shared by {attribution} on Discord" instead of profile link (both selects gained `attribution`) + audit labels `capture.published/discarded/claimed`. Anon renders "a community member". Original spec: "Captures" section on existing `/communities/[slug]/moderation` page (granted-unfiled list, consent badge, Discord link) → *Publish as answer to… (question picker)* / *Publish as new question* / *Discard*. Published rows authored by **Silas system user** with `attribution` = Discord name (or "a community member" if anon), **pre-approved — no second queue** (mod curated by capturing). Question page renders "📚 Shared by {name} on Discord · archived by Silas!" (mirrors Plish system-user span pattern).
  - **P4 — Claim funnel: ✅ CODE BUILT 07/23, tsc clean, NOT yet committed** — `middleware.ts` sends logged-out `/claim/{token}` → `/signup?redirect=/claim/{token}` (token survives auth); `app/(app)/claim/[token]/page.tsx` + `components/community/ClaimCapture.tsx`; `claimCapture(token)` in captures.ts re-attributes published content (author_id/asker_id=claimer, attribution=null) or pre-registers claimed_by on unfiled captures (publish actions then use claimed_by ?? SILAS). Declined captures unclaimable; second claim blocked. Changelog 07-23 entry added (member-facing). Deploy order agreed: Stoke push → Silas push → Sean sets 4 Railway env vars → `railway run npm run deploy` → live capture test. Original spec: route `/claim/[token]` — logged-out → signup w/ redirect; logged-in → action verifies token, sets author_id = claimer, clears attribution, marks claimed_by, revalidates. Works months later. Identity proof = token possession (only ever sent to their Discord DM/mention); no Discord OAuth v1 (noted soft spot, acceptable).
- **✅ DEPLOYED 07/23 (both sides pushed — Bun crashed mid-"go", recovered next session, nothing lost):** Stoke side = commit **`de58d9d`** on main (12 files: P3+P4 — captures.ts, CaptureActions/ClaimCapture components, moderation-page section, gear badge, question-page attribution, /claim/[token], middleware redirect, audit labels; tsc clean; left CLAUDE.md/.claude/PDF/images OUT per convention). Silas side = commit **`0589494`** on `main` in the Silas repo (4 files: src/capture.js, index.js routing, deploy-commands.js, .env.example; `node --check` clean; stray `bash.exe.stackdump` left untracked). Both auto-redeploying on Railway.
  - **⚠️ Silas Railway project = `energetic-possibility` (`6c165f2c-0090-445f-83b3-ece17625ec0f`), service `bodydoubling-pomodoro-bot` (`32470761-1269-4541-a1f1-944cc0e7187c`) — NOT `melodious-purpose` (that's Plish).** CLAUDE.md never recorded Silas's Railway project name before; now it does.
  - **⚠️ THE ENV-VAR GOTCHA (07/23, first live test failed):** the capture context command registered fine + appeared in Discord, but clicking it returned **"⚠️ Capture isn't configured on this bot (missing Stoke env vars)."** Root cause = the 4 Stoke vars were **never added** to the Silas service (`railway variables` showed ONLY the pre-existing welcome-feature vars: CHECKIN_CHANNEL_ID/DATA_DIR/DISCORD_TOKEN/CLIENT_ID/GUILD_ID + RAILWAY_*). **KEY LESSON: command registration (`railway run npm run deploy`) is SEPARATE from runtime env — deploy-commands.js only needs DISCORD_TOKEN/CLIENT_ID/GUILD_ID, so a successful registration proves NOTHING about the Stoke vars.** `capture.js` `captureEnabled()` = `Boolean(SUPABASE_URL && SUPABASE_SERVICE_ROLE_KEY && STOKE_COMMUNITY_ID)` (STOKE_URL optional → defaults stoke.community; CAPTURE_MOD_ROLE_ID optional → falls back to ManageMessages perm gate). Fix given: run in the Silas repo terminal `railway variables --set "SUPABASE_URL=https://gzssbicdblkmllutegju.supabase.co" --set "SUPABASE_SERVICE_ROLE_KEY=$key" --set "STOKE_COMMUNITY_ID=5310e8c7-1276-485f-b77e-406d7edcf890" --set "STOKE_URL=https://stoke.community"` (PS one-liner reads $key from Stoke's `apps/web/.env.local` so the secret never hits the transcript). `railway variables --set` auto-triggers a redeploy. **Stoke Supabase URL = `https://gzssbicdblkmllutegju.supabase.co`.** ⏳ PENDING: Sean sets the vars + re-tests the capture → consent DM → moderation-page landing to prove end-to-end.
- **Consent properties (the heart):** consent = button click tied to their Discord account, timestamped, stored beside the content; nothing publishes while pending; declines terminal + recorded; anon first-class; credit upgrades to real profile only via their own claim action.
- **Defaults taken (Sean can override):** no auto-expiry on pending (mod re-nudges manually); revocation honored manually via ticket/DM for v1; Discord mod gate = by Discord role id.
- **Also discussed, parked:** Phase-3-style retrieval (`/library` slash command in Discord — Silas searches Stoke KB, returns links = funnel in reverse) as a fast follow AFTER capture ships; Silas features INSIDE Stoke (pomodoro/chimes) = NO (voice out of v1 scope).
- **Effort est:** P0+P1 ~1hr; P2 ~1 day (bot work, in the Silas repo `C:/Users/Sean/Desktop/DiscordBot/bodydoubling-pomodoro-bot/pomodoro-bot`); P3 half-day; P4 small.
- **Session context note (07/23):** the prior session CRASHED mid-discussion of this idea — **Bun v1.4.0 segfault at 0x0** (Claude Code runs on Bun; "bug in Bun, not your code"), after ~3.6hrs elapsed; followed by a TUI render storm (stacked repaints at shrinking widths — cosmetic only, state intact). If CC crashes hard again: check for the bun.report link in scrollback.
- **⚠️ ACTUAL `discord_captures` column names (verified live 07/24, CORRECTS the P1 spec above):** the author column is **`discord_author_name`** (NOT `author_name`) and the timestamp is **`created_at`** (NOT `captured_at`); consent times are `consent_asked_at`/`consent_answered_at`. Full row also has `photos text[]` (see below), `claim_token`, `claimed_by`, `question_id`, `answer_id`.

## Capture Photos (07/24/2026 — SHIPPED, Stoke `fa22904` + Silas `25eb41d`)
- **Trigger:** Sean, re the MsNerdyBirdy capture: *"there was a photo in the post that MsNerdyBirdy made - is there a way to upload that to Stoke as part of the 'capture for stoke' process"* → chose **full image support** (over one-off / answers-only).
- **⚠️ THE HARD PART — Discord CDN attachment URLs are SIGNED + EXPIRE (~24h)** (`?ex=&is=&hm=`), so you CANNOT store the raw Discord link. Silas **downloads each image attachment at capture time and re-uploads it to Stoke's public `avatars` bucket** at path `discord-captures/{messageId}/{i}-{rand}.{ext}`, storing the durable public URL. (`reuploadImages()` + `storageUpload()` helpers in `src/capture.js`; POST `${SUPABASE_URL}/storage/v1/object/avatars/{path}` with service key + `x-upsert:true`; public URL = `/storage/v1/object/public/avatars/{path}`.)
- **Migration `20260724000000_capture_photos.sql`** (RUN live 07/24, verified): adds `photos text[] not null default '{}'` to **`discord_captures` + `kb_questions` + `kb_answers`** (same pattern as `bulletin_posts.photos`/`events.photos`).
- **Image-only captures now allowed** — the old *"has no text content — I can only archive text for now"* guard now rejects only a post with NEITHER text NOR image; the consent DM acknowledges the image(s) and renders even with empty text. `content text not null` still holds (empty string OK for image-only).
- **Stoke side:** `captures.ts` `loadFilableCapture` selects `photos` + `publishCaptureAsAnswer`/`publishCaptureAsQuestion` carry `photos` onto the filed row; moderation page + `questions/[questionId]/page.tsx` render `<PhotoGallery photos={…}>` (galleries placed OUTSIDE the `EditQuestion`/`EditAnswer` wrappers so they show in both view + edit mode). Changelog 07-24 entry added (member-facing).
- **MsNerdyBirdy backfill (07/24, done):** her capture was filed BEFORE photo support so her image was lost. **GOTCHA: the bot CANNOT REST-`GET /channels/{id}/messages/{id}`** for that channel — returns **403 Missing Access (code 50001)** because the bot lacks View Channel/Read History there; a **context-menu capture works anyway** because the interaction hands the bot the *resolved* `targetMessage` (no channel read perm needed). Workaround used: Sean right-clicked the image → Copy Link → pasted the fresh signed CDN URL → a local script (Stoke service key from `.env.local`) downloaded it, uploaded to storage, PATCHed `photos` on question `e5551f42…` + the capture row. **TIP: strip Discord's `format=webp&width=&height=` query params (keep `ex`/`is`/`hm`) to fetch the FULL-RES original** instead of the resized thumbnail.
  - **⚠️ UPDATE 07/26 — that filed question NO LONGER EXISTS.** Audit-log timeline: `capture.published` 07/24 13:56:43 (question `e5551f42-365d-4031-afc4-9d2ede6d1a69`, capture `1f15859b-1b54-42aa-984e-9cda69cda9dd`) → backfill JPEG uploaded 15:58:50 → **`question.deleted` 16:03:37 (by Sean)** → **`capture.discarded` 16:11:10**. So the capture DID work exactly as recorded here; the rows are gone because the question was deleted ~5 min after the backfill and the capture discarded 8 min later, almost certainly swept up in test-cleanup. **MsNerdyBirdy is one of Sean's Discord MODERATORS.** Her photo is still in storage at `avatars/discord-captures/1529634031962951680/backfill-0.jpg` (70,048 bytes) — the text is gone with the row.
    - **⚠️ I INSPECTED THE IMAGE 07/26 and it REVERSED the conclusion above.** It is a mobile Discord screenshot of the **BodyDoubling.com Test Server** channel sidebar (15 members, NOT the main 13k server), with **`#voice-channel-description` selected** above `#faq`, and the category tree below it (Event Hub · 📊 Server Stats · ⚙️ Moderation · Mod Communications VC · 🎟 Mod Communications Tickets · 📄 Server Support Squad EOI · 🎫 Tickets · ☕ The Coffee Area · 💬 General · Community Voice → `#preview-group`). So it is a **UI/config screenshot from a mod working conversation, NOT durable advice** — most likely her showing the voice-channel-description channel existing (ties to the still-open Silas item about that channel going live). **Deleting it on 07/24 therefore looks like correct curation, not an accident.** Do NOT re-capture it. Caveat: only the IMAGE survives, so if her post's TEXT carried real advice that can't be ruled out from the artifact alone.
    - **⭐ LESSON (cost me a wrong recommendation):** I argued from METADATA — real moderator + credited consent + a filed question — to "lost member content worth restoring", and recommended a re-capture, without opening the artifact that was sitting in storage the whole time. **When the artifact is retrievable, LOOK AT IT before characterizing what was lost.** Storage outlives the row; a public `avatars` URL + the `Read` tool is all it takes.
- **Discard DELETES the capture row (07/26):** `capture.discarded` leaves ONLY the audit event — both discarded capture ids (`1f15859b`, `2a4b696f`) are absent from `discord_captures`. This is DIFFERENT from a **declined** capture, which is kept as the record that consent was asked and refused. Don't conflate the two.
- **Delete-then-discard walks around the "discard blocked once published" guard (07/26):** deleting the filed question nulls `question_id` via `ON DELETE SET NULL`, so the capture no longer looks published and `discard` succeeds. "Published" is therefore not a durable lock on a capture. (The later `dismissed_at` fix `caadb7f` stops a deleted-content capture from re-entering the queue, but does NOT stop discard.)
- **LESSON (recurring):** a notes-vs-DB "discrepancy" is often neither a bug nor a bad note — the note was true when written and reality moved on. **Check `audit_log` FIRST**: it outlives the rows it describes and reconstructs the whole timeline (`action=like.capture*`, plus `question.deleted`/`photo.*`). Storage objects outlive deletions too, so a surviving upload proves an action really happened.
- **Silas repo hygiene (07/24, `99648b9`):** `.gitignore` gained `.claude/`, `*.stackdump`, `background.jpg`; deleted the stray `bash.exe.stackdump`.

## Photos Tab + Multi-image readiness (07/24/2026 — SHIPPED `f745383`)
- **Trigger:** Sean wanted "a place per community that has all photos… like the community gallery but organizers and moderators can also see a gallery section for all post pictures or photos from the chats etc." → "consolidate any in-line photos and have a different view for moderators and organizers vs. the community members."
- **Decisions (AskUserQuestion):** placement = **new "Photos" tab** in the community tab strip (now 6 tabs); members see **curated gallery only**; the **"All photos" aggregate is organizer/moderator-only** (some inline images come from role-gated channels a member can't see).
- **Impl** (`communities/[slug]/page.tsx`): moved the curated gallery OFF the landing INTO the Photos tab (true consolidation; preview page unaffected — separate file). Photos tab shows curated gallery for all; for mods a mod-only `photoWall` aggregate = every inline image (bulletin/events/Q&A incl captured / chat), each with a source label + back-link, newest first. New client `components/community/CommunityPhotoWall.tsx` (lightboxed grid, hover source label). Tab is conditional: mods always; members only when a curated gallery exists (never empty). Changelog 07-24.
- **Multi-image future-proofing (Sean: "pre-assume that multiple images per post are going to happen and build with that in mind"):** migration `20260724010000_message_photos.sql` adds `messages.photos text[]` (bulletin/events/Q&A already had `photos[]`); the aggregate reads BOTH `image_url` (legacy single) AND `photos[]` so a future multi-image chat composer needs no rework. The composer UI itself is NOT built (data model + aggregate only).
- **PostgREST validated:** `.or('image_url.not.is.null,photos.neq.{}')` — the `photos.neq.{}` empty-array clause parses fine (tested against a real `photos[]` column).

## Deleting captured content: return-nav + dismiss (07/24/2026 — `09fac5c`, `caadb7f`)
- **Delete-question blank-page bug (`09fac5c`):** deleting a question from its own detail page called `router.refresh()` and stayed on the now-404 URL → blank page w/ only the top bar. Fixed: `DeleteItemButton` gained an optional `redirectTo`; the question delete passes `?tab=qa`. Answer deletes stay refresh-in-place (question page still exists). Pre-existing bug, not capture-specific.
- **Captured-content delete = dismiss, not re-queue (`caadb7f`):** a `discord_captures` `question_id`/`answer_id` uses FK ON DELETE SET NULL, so deleting the filed content nulled the link and the granted capture BOUNCED BACK into the moderation "Discord captures" queue. Sean chose **"Keep record, don't re-show."** Migration `20260724020000_capture_dismissed.sql` adds `dismissed_at`; `deleteQuestion`/`deleteAnswer` set `dismissed_at` on the linked capture BEFORE deleting; the moderation list + gear-badge count both add `.is('dismissed_at', null)`. Consent record preserved, no re-show.
- **Re-capture a declined/dismissed message (Silas `1826f9f`):** `discord_message_id` is UNIQUE → a 2nd capture 409s. Silas now, on 409, looks up the existing row and if it's `declined` OR `dismissed_at` set, **revives it to pending** (re-asks consent); granted/filed captures still block as a duplicate. Helpers `getCaptureByMessage`/`reviveCapture` in `src/capture.js`.

## Photo Audit — itemized photo.added / photo.removed (07/24/2026 — `e9626c9` app-level + `3097f95` chat trigger)
- **Trigger:** Sean: "is adding/removing photos part of the audit log?" (was: no). Chose **Full — dedicated itemized events with source** (over count-in-existing).
- **Reality mapped first:** only the **community gallery** has a true edit-photos path (`updateCommunityInfo` → real before/after diff); bulletin/events set photos at CREATE only (no edit action); Q&A photos come only from Discord captures; **chat sends are CLIENT-SIDE (direct insert, no server action)** so chat needs a DB trigger, not app code.
- **App-level (`e9626c9`):** new `logPhotos({actorId, communityId, added?, removed?, source, parentId})` + `PHOTO_SOURCE_LABELS` in `lib/audit.ts`. Wired: gallery diff (`community.ts`), bulletin `submitPost`/`deletePost`, events `createEvent`/`deleteEvent` (removed photos deduped across a series via a pre-delete photos fetch), captures `publish*` (added), knowledge `deleteQuestion`/`deleteAnswer` (removed). `qa_answer` uses `parentId = questionId` so View→ links to the question. Both audit renderers (`AuditLogClient.tsx` + settings `page.tsx`) render a 48px thumbnail + source label + a photo-aware `targetLink`.
- **Chat trigger (`3097f95`, migration `20260724030000_chat_photo_audit.sql`):** `security definer` `log_message_photos()` on `messages` — INSERT→added, soft-or-hard delete→removed (actor = `deleted_by`), restore (deleted_at cleared)→re-added; iterates `image_url` + `photos[]`; metadata `{source:'chat', url, parent_id: msgId, channel_id}`; looks up `community_id` from `channels`. Three triggers ins/upd/del. **VERIFIED LIVE 07/24** — real post logged added→removed→(restore re-added), Q&A add+remove also confirmed. No changelog (audit is staff-only).
- **GOTCHA (not a bug):** the trigger does NOT backfill — a message CREATED before the migration logs only its removal (its insert predated the trigger); only post-migration inserts log `photo.added`.
- **⚠️ `discord_captures` real cols (re-confirmed):** `discord_author_name`, `created_at`, `dismissed_at`, `photos text[]`.

## Moderator Guide: Capturing Discord posts (07/25/2026 — BUILT, tsc clean, NOT yet committed)
- **Trigger:** Sean was mid-task on *"a pdf and webpage post for the Discord / Stoke moderators to learn how to use Silas!' capture to Stoke"*. Found in the working tree: untracked `apps/web/public/moderator-guide-silas-stoke.{html,pdf}` written earlier the same morning (07:41) — content COMPLETE (30-second version, prerequisites, 9 who-acts-colour-coded steps, 3 consent-outcome cards, troubleshooting table, do/don't etiquette, printable quick-reference) but with **10 empty screenshot slots** and nothing committed.
- **Decisions (AskUserQuestion):** screenshots = **Sean shoots them, Claude embeds** (the right-click Apps-menu shot can't be faked); hosting = **staff-gated page like `/guide`**, NOT a public static file.
- **NEW page `apps/web/app/guide/capture/page.tsx`** — ports the HTML to Tailwind/JSX with the **same server-side staff guard as `/guide`** (`community_members` role in organizer/moderator + status active via admin client, OR any `platform_roles` row; else redirect `/login` or `/home`). `middleware.ts` has NO public-allowlist entry for `/guide`, so `/guide/capture` inherits the auth gate too (defence in depth). Local `Section`/`Note`/`Warn`/`Good`/`Step`/`Shot` subcomponents; `whoStyles` maps mod→violet, author→cyan, silas→yellow.
- **`Shot` component takes an OPTIONAL `src`** — renders a labelled "Screenshot coming" placeholder until the image is supplied, so the page ships readable with partial or zero screenshots. Target dir `apps/web/public/guide/capture/`, filenames `01-message` `02-apps-menu` `03-confirm` `04-consent-dm` `05-answered` `06-gear-menu` `07-captures-queue` `08-filing` `09-published` `10-claim`.
- **`AppNav.tsx`:** gear menu gained a "Capturing Discord posts" → `/guide/capture` link, wrapped alongside "Organizer guide" inside the existing `isCommunityStaff` gate (the single `<Link>` became a fragment with two).
- **⚠️ MOVED `moderator-guide-silas-stoke.{html,pdf}` from `apps/web/public/` → `docs/`.** Anything in `public/` is served to anyone with the URL (that's why `.pdf`/`.html` are in the middleware matcher exclusion list) — leaving it there would have contradicted the staff-gating choice. The PDF is now handed out by **attaching it directly in Discord mod-chat** (no hosting, no URL); the HTML stays as its render source.
- **Nice side effect:** gating made the "you need a Stoke organizer/moderator account" prerequisite self-proving — reworded to *"If you're reading this page, you have it."*
- **⏳ OPEN:** (1) Sean shoots the 10 screenshots; (2) screenshot files in `public/guide/capture/` would themselves be PUBLIC even though the page is gated — offered a guarded route handler if that matters; (3) the draft PDF still contains the placeholder boxes + internal "Shot notes:" directions — strip both and regenerate once screenshots land. Staff-only tool → **NO changelog entry** per the standing rule.
- **✅ ALL THREE CLOSED + SHIPPED same day (07/25) — commits `9b7befd` then `504f65e`, PUSHED (`3097f95..504f65e`, `main == origin/main`).** Live at **`https://stoke.community/guide/capture`**. PDF is NOT hosted — handed out by attaching `docs/moderator-guide-silas-stoke.pdf` in Discord mod-chat.
  - **Screenshots are GATED, not in `public/` (Sean chose the harder option over my "public/ simple (Recommended)").** Files live in **`apps/web/guide-shots/capture/`** (outside `public/`, nothing serves them implicitly); NEW route **`app/guide/capture/shots/[name]/route.ts`** serves them — 404 (not 403) for non-staff, `SAFE_NAME` regex allowlist, `path.dirname(filePath) !== SHOTS_DIR` traversal guard, `Cache-Control: private, max-age=3600`. Page `src`s are `/guide/capture/shots/NN-name.png`.
  - **NEW `lib/guide-access.ts` `getGuideAccess()` → `{userId, isStaff}`** — the staff check EXTRACTED out of `page.tsx` so the page and its asset route share ONE gate. Rationale worth keeping: *a gated page whose images are ungated is not gated.* Reuse this for any future gated-asset page.
  - **⚠️⚠️ THE PROD-ONLY TRAP — `output: 'standalone'` only bundles files Next can TRACE, and images read via `fs` are INVISIBLE to the tracer.** Without a declaration they work locally and **404 in production only**. Fixed with `outputFileTracingIncludes: { '/guide/capture/shots/[name]': ['./guide-shots/**'] }` in `next.config.ts`; verified present at `.next/standalone/apps/web/guide-shots/capture/`. **Any future file served via `fs` from a route handler needs this.**
  - **PRE-EXISTING BUG FOUND, deliberately NOT fixed:** the `postbuild` script copies to `.next/standalone/.next/static`, but the monorepo standalone output actually nests at **`.next/standalone/apps/web/.next/`** → postbuild FAILS on a local `npm run build`. Proved pre-existing by `git stash push -- next.config.ts` + rebuilding → identical failure. Railway deploys succeed, so its env resolves differently; left alone rather than "fix" a script working where it counts. **Method worth repeating: when a build breaks after your change, revert ONLY your change and rebuild before claiming or denying ownership.**
  - **`CommunityGear` badge only renders when `pendingCount > 0`** (`CommunityGear.tsx` lines 53 icon + 70 row) — so the gear-badge screenshot is ONLY shootable in the window AFTER consent is granted and BEFORE filing (a granted-unfiled capture is what makes `totalPending` = 1). I twice told Sean it was "shootable any time" — wrong, and Body Doubling's queue was at 0 across all six sources.
  - **Claiming a capture NULLS its attribution** — `claimCapture` sets `author_id` = claimer and `attribution` = null, so the "📚 Shared by X on Discord" line disappears BY DESIGN and the post renders as ordinary self-authored content. Don't misread that as "the capture didn't work." (Cost a wrong first read of one of Sean's screenshots this session.)
  - **PDF regeneration recipe (repeatable):** python pass over `docs/moderator-guide-silas-stoke.html` → base64-EMBED each shot into its empty `shot-frame` div (self-contained: the HTML is in `docs/` but images are in `apps/web/guide-shots/`, so a relative path would break); strip the 10 internal `<span class="tip">Shot notes:…</span>`; replace the `<div class="shot-cap">` "What to capture: …" SHOOTING INSTRUCTIONS with the real captions copied from `page.tsx` (one source of wording for page + PDF); strip the "Screenshot N" labels. **Substitute in REVERSE document order** — forward edits shift later match offsets. 562 KB HTML → **771 KB PDF**, `%PDF-1.4` verified. Edge flags per the global tooling note; the `fallback_task_provider.cc` ERROR spam is BENIGN, the PDF still writes.
  - **Read-only Supabase probes with ZERO deps:** a scratchpad `.mjs` CANNOT `import '@supabase/supabase-js'` (resolves from the SCRIPT dir, not cwd) → use plain `fetch` on `${SUPABASE_URL}/rest/v1/…` with `apikey` + `Bearer` headers. Exact counts via headers `Prefer: count=exact` + `Range: 0-0`, then read `content-range`.
  - **Live capture state after the demo run:** 4 `discord_captures` rows, ALL authored `Sean🌻`. Demo question `c88f8ac2` "What helps you continue a task?" is published with `attribution = 'Sean🌻'` and **still unclaimed** (`/claim/f8294c7a60a74c88b2656d8ba2ea1fe2`). **✅ RESOLVED 07/26 — the MsNerdyBirdy capture WAS filed as recorded; its question was deleted 07/24 16:03 and the capture discarded 16:11 (discard deletes the row), so only the audit trail remains.** Full timeline + implications in the Capture Photos section above. Live state 07/26: question `73ff9f3a` ("TEST!") also deleted 07/26 04:39 ET during cleanup.
  - Optional cleanup left to Sean: the `#moderator-only` test post + the "What helps you continue a task?" question.

